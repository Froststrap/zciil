[default]
extend-ignore-re = [
  'cl\[ist]',
  '\[lan\]guage',
  '"ba"',
  '_hda_',
  # macOS version
  "Big Sur",
]
check-filename = true

[files]
ignore-files = true
ignore-hidden = false
extend-exclude = [
  ".hg/",
  ".git/",
  "*.patch",
  # Contributor names aren't typos.
  ".mailmap",
]
